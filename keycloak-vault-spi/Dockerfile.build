FROM maven:3.8-openjdk-17 AS builder
WORKDIR /build
COPY pom.xml .
COPY src ./src

# First list the structure for troubleshooting
RUN find . -type f | sort

# Run Maven build with verbose output
RUN mvn clean package -X
